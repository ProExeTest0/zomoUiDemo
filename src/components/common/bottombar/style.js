import {colors} from '../../../helpers/colors';
import {hp, wp} from '../../../helpers/constants';
import {StyleSheet} from 'react-native';

export const bottomStyles = StyleSheet.create({
  bottombarIcons: {
    height: hp(20),
    width: wp(20),
    tintColor: colors.greenA7CCD5,
  },
});
